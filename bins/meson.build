bins_dir = meson.global_source_root() / get_option('bins')
bins = {
  'mwccarm': files(bins_dir / 'mwccarm.exe'),
  'mwasmarm': files(bins_dir / 'mwasmarm.exe'),
  'mwldarm': files(bins_dir / 'mwldarm.exe')
}
